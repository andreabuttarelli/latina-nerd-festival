# Sanity — public (browser-safe) read config
PUBLIC_SANITY_PROJECT_ID=your_project_id
PUBLIC_SANITY_DATASET=production

# Sanity — server-only write token (Editor/Write permissions). NEVER expose to client.
# Create one at https://sanity.io/manage → API → Tokens
SANITY_WRITE_TOKEN=
