import festival from './festival';
import event from './event';
import mission from './mission';
import userProgress from './userProgress';

export const schemaTypes = [festival, event, mission, userProgress];
